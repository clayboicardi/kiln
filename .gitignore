# IDE
.idea/
.fleet/
.vscode/
*.iml
*.ipr
*.iws
out/

# Claude Code session-local state (transcripts, local overrides, custom skills until promoted)
.claude/*
# Exception: workspace-shared CC config (kotlin-lsp enable, etc.) — see docs/decisions/2026-05-21-tooling-recommendation.md
!.claude/settings.json
# Exception: workspace-shared skills (Tier 2 picks — once authored)
!.claude/skills/

# Gradle (when added in MVP Sessions 1-3)
.gradle/
build/
local.properties
gradle/wrapper/dists/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log

# Environment
.env
.env.local

# Native build output (when AAudio/WASAPI phase-2b lands)
*.dll
*.so
*.dylib
cmake-build-*/
# Exception: vendored native libraries (BSD-3 libFLAC + future siblings)
!audio/playback/src/desktopMain/resources/native/**/*.dll
!audio/playback/src/desktopMain/resources/native/**/*.so
!audio/playback/src/desktopMain/resources/native/**/*.dylib

# Keystores (Android, when signing comes in)
*.jks
*.keystore
keystore.properties

# Coverage reports
htmlcov/
coverage/
.nyc_output/

# JetBrains
.kotlin/
