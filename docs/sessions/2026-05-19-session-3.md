# Session 3 — Pre-MVP Gate Clear + Tooling Verification

**Date:** 2026-05-19 (single sitting; ~30 min)
**Session goal:** Walk Clay through Pre-MVP gate review, confirm decisions, verify Pre-Session-1 tooling state.
**Outcome:** Pre-MVP gate **CLEARED**. All 12 vetting items explicitly acknowledged. Tooling install state captured. Ready for MVP Session 1 to start in a fresh Claude session.

This is the per-plan-§11 session closeout note. Sibling to `2026-05-18-session-2.md` (Pre-MVP Research close-out).

---

## What was done

A short follow-up session to session 2's documentation marathon. Five Q&A clarifications + four environment checks + one UUID5 computation + two doc edits.

### Q&A clarifications resolved

| # | Question | Resolution |
|---|---|---|
| 1 | "Wix - I already built clayboicardi.com on Cloudflare, can we use that?" | Name collision: **WiX Toolset** (Windows MSI compiler) ≠ Wix.com (website builder) ≠ Cloudflare hosting. WiX Toolset is required at build time for `:app-desktop:packageMsi` per Item 10 — Cloudflare is unrelated (would only be relevant if hosting the MSI download). |
| 2 | "Temurin 25 vs Temurin 21 vs openjdk 21.0.10" | Spec §2 hard-locks JVM 21 = target bytecode level, not dev/runtime JDK. CLAUDE.md anchor of "Temurin 21" is the JAMZ-tested config; the load-bearing rule is **not JBR**. Verified Clay's `openjdk version 21.0.10` IS Temurin (build 21.0.10+7-LTS). Zero install needed. |
| 3 | "Doc says android-21 needed — please confirm accuracy" | Doc was wrong. `minSdk = 21` is manifest metadata, NOT a build prerequisite. Only the `compileSdk` platform (android-36) is required for compilation. Clay's SDK is build-ready as-is. Doc corrected with append-only CORRECTION callout in §2c. |
| 4 | "For UUID5, can I paste this myself?" | Yes — computed via pure PowerShell .NET SHA-1 (Clay's Python was mid-version-mismatch-resolution): `611fd94b-756e-561d-ba94-af658a225268`. Algorithm deterministic; same value as Python `uuid.uuid5(...)` would produce. Saved to engram under `kiln/msi-upgrade-uuid`. |
| 5 | "Music library lives at D:\tiddl" | Noted; overrides scaffold prep §3c default of `%USERPROFILE%\Music`. Saved to engram under `kiln/clay-environment/music-library-root`. |

### Pre-MVP gate acknowledgment captured

- ✅ **§1a Repo name:** `clayboicardi/kiln`, **public** day-one (Software-as-Self-Portrait pattern, plan §3.1)
- ✅ **§1b + §4 All 12 review prompts:** explicit acknowledgment, no push-back
- 🔲 **§3b App icon:** Clay's explicit defer to MVP Session 26-28 polish

### Tooling install state (post-verification)

| Tool | Status | Detail |
|---|---|---|
| JDK 21 Temurin | ✅ Verified | `Temurin-21.0.10+7 (build 21.0.10+7-LTS)` — passes "not JBR" rule, matches JAMZ-tested anchor distribution |
| `gh` CLI auth | ✅ Verified | Logged in as `clayboicardi`, scopes include `repo`, `admin:org`, `delete_repo`, `workflow` |
| Android SDK | ✅ Verified | `android-35`, `android-36`, `android-36.1` + `build-tools/36.1.0` — sufficient for `compileSdk = 36` |
| WiX Toolset | 🔲 Pending | Install before MVP Session 3 packageMsi smoke test (~2 sessions of buffer) |
| Chocolatey | Not present | Alternative install paths offered for WiX (winget recommended on Windows 11) |

### Doc fixes

- `docs/scaffold/2026-05-18-clay-action-items.md` §2c — added append-only CORRECTION callout noting android-21 is not a build prerequisite
- `docs/scaffold/2026-05-18-clay-action-items.md` — appended §7 Status Log marking gate clear + tooling verification

---

## What works (verified this session)

- ✅ Clay's JDK is `Temurin-21.0.10+7 (build 21.0.10+7-LTS)` — passes CLAUDE.md "not JBR" rule
- ✅ Clay's `gh` CLI authenticated with full repo-creation scopes — ready for MVP Session 1 Step 1
- ✅ Clay's Android SDK has android-36 + recent platform + build-tools — sufficient for compileSdk 36 builds
- ✅ UUIDv5 for MSI upgrade detection computed and saved: `611fd94b-756e-561d-ba94-af658a225268`
- ✅ Clay's repo decision captured: `clayboicardi/kiln`, public day-one
- ✅ All 12 Pre-MVP review prompts acknowledged with no push-back — plan §2.2 exit criteria met

---

## What does NOT work yet (open items)

### Pre-Session-1 tooling install (Clay's interactive shell)

1. **🔲 WiX Toolset 3.x install.** Three install paths offered:
   - **winget** (Windows 11 default, recommended): `winget install WiXToolset.WiXToolset` — should pull WiX 3.x; verify post-install that `light.exe` and `heat.exe` are on PATH
   - **Chocolatey** (requires installing Chocolatey first per https://chocolatey.org/install in admin PowerShell): then `choco install wixtoolset`
   - **Manual MSI**: download from https://wixtoolset.org/releases/ (WiX 3.x only — 4.x is NOT jpackage-compatible per Item 10 vetting)

   WiX install is not blocking for MVP Session 1 or 2 — only required before MVP Session 3's packageMsi smoke test. ~2 sessions of buffer.

### Carried-forward JIT items (unchanged from session 2 closeout)

See `docs/sessions/2026-05-18-session-2.md` "Carried-forward JIT items" table — all entries still apply. None resolved this session.

---

## Next session's first action

**Fresh Claude session in `C:\Users\chawo\Projects\kiln\` for MVP Session 1.**

Cold-read budget (~620 lines total):

1. `kiln/CLAUDE.md` (~95 lines) — project orientation
2. `docs/sessions/2026-05-19-session-3.md` (this file, ~140 lines) — gate-clear state + tooling status
3. `docs/scaffold/2026-05-18-mvp-session-1-prep.md` §1 + §12 (~200 lines of the 718-line doc — §1 one-page summary + §12 step-by-step scaffold sequence)
4. Optional: `mem_search "kiln pre-mvp gate-cleared"` to warm engram

Then `TaskCreate` for the 16 numbered scaffold steps from prep §12 and start work at Step 1 (GitHub repo creation: `clayboicardi/kiln`, public).

---

## Engram memory written this session

| Topic key | What |
|---|---|
| `kiln/msi-upgrade-uuid` | UUIDv5 result `611fd94b-756e-561d-ba94-af658a225268` + derivation chain |
| `kiln/clay-environment/music-library-root` | `D:\tiddl` override of default `%USERPROFILE%\Music` |
| `kiln/pre-mvp-gate-cleared-2026-05-19` | Gate-clear decision record + tooling status |

Plus this session's `mem_session_summary` (saved at session end).

---

## Session 3 in numbers

- **Duration:** ~30 min
- **Commits:** TBD (housekeeping commits offered to Clay; not auto-committed pre-approval)
- **Decisions clarified:** 5 (Clay's questions on WiX, JDK, Android SDK, UUID5, music library)
- **Decisions captured:** 3 (repo name, full Pre-MVP ack, app icon defer)
- **Doc edits:** 2 (`clay-action-items.md` §2c correction + §7 status log append)
- **Memory entries saved:** 3 (UUID5, music library, gate-clear)
- **Pre-MVP gate state:** **CLEARED ✅**

---

**End of Session 3.** Pre-MVP gate cleared; MVP Session 1 ready to begin in a fresh Claude session at Clay's discretion.
