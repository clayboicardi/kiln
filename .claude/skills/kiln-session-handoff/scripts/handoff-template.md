# Session {{session_num}} Handoff — {{title_tail}}

**Authored:** {{date}} (end of Session {{prev_session_num}})
**For:** The next Claude session (Clay-driven; can be a fresh cold-context session)
**Goal:** {{summary_line}}

---

## 🚀 Pre-flight (first 5 minutes of the session)

**Read order (cold-start):**

1. This file — full read.
2. `docs/sessions/{{date}}-session-{{prev_session_num}}.md` — Session {{prev_session_num}} closeout, if written.
3. `CLAUDE.md` — project orientation + cumulative gotchas.
4. *Optional* `mem_search "kiln"` to surface engram context for the picked Track.

**Confirm clean baseline:**

```powershell
cd C:\Users\chawo\Projects\kiln
git log --oneline -5           # expect {{head_sha_short}} at HEAD
git status                     # expect: {{tree_state_one_line}}
just verify                    # canonical 5-target build — expect ~5-15s incremental + tests green
```

If baseline is dirty or build red — STOP, diagnose, surface to Clay.

---

## Where we are (current state, {{date}} {{time_block}})

**Repo:** https://github.com/clayboicardi/kiln (public, Apache 2.0)
**Branch:** `{{branch}}` at commit `{{head_sha_short}}`. Origin sync: {{origin_sync_state}}.
**Build:** {{build_state_line}}
**Tests:** {{test_count_summary}}

---

## Session {{prev_session_num}}'s outputs

Commits since the prior handoff ({{commit_count}} total, {{commit_range}}):

{{commit_table}}

---

## In-flight items (handoff)

{{in_flight_section}}

---

## Decisions made

{{decisions_section}}

---

## Gotchas discovered (worth promoting to CLAUDE.md?)

{{gotchas_section}}

If any of these warrant CLAUDE.md promotion, invoke `/claude-md-management:revise-claude-md` separately — kiln-session-handoff does NOT modify CLAUDE.md.

---

## Working tree state at handoff

```
{{tree_state_full}}
```

Stash list:
```
{{stash_state}}
```

---

## Verify before starting work next session

- [ ] `just verify` — passes clean on `main`
- [ ] Read CLAUDE.md for any new rules
- [ ] Check this handoff's "In-flight items"
- [ ] Resolve any uncommitted state shown above

---

## References

- Plan: `docs/superpowers/plans/2026-05-18-kiln-execution-plan.md`
- Spec: `docs/superpowers/specs/2026-05-18-kiln-rebuild-design.md`
- Tooling decisions: `docs/decisions/2026-05-21-tooling-recommendation.md`
- Library vetting: `docs/decisions/2026-05-18-library-vetting.md`
- Prior handoff: `docs/sessions/{{prev_handoff_filename}}`

---

## 📋 Copy-paste prompt for the next session

```
Read docs/sessions/{{date}}-session-{{session_num}}-handoff.md and execute it as
your prompt for this session. Pick up the listed in-flight items, scope a coherent
batch, then implement + push at session close.
```

---

**End of Session {{session_num}} Handoff.** Generated by `kiln-session-handoff` skill {{generator_version}}.
